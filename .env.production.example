# -----------------------------------------------------------------------------
# PRODUCTION ENVIRONMENT VARIABLES TEMPLATE
# Rename this to .env.local for local testing or add these to Vercel Dashboard.
# -----------------------------------------------------------------------------

# DATABASE (Supabase)
# Use Transaction Pooler URL (port 6543) for serverless stability
DATABASE_URL="postgres://[user]:[password]@aws-0-[region].pooler.supabase.com:6543/[db-name]?pgbouncer=true"
# Direct connection (port 5432) required for migrations
DIRECT_URL="postgres://[user]:[password]@aws-0-[region].supabase.co:5432/[db-name]"

# AUTHENTICATION (NextAuth.js)
# Generate with: openssl rand -base64 32
AUTH_SECRET=""
# The full URL of your deployment (e.g., https://crochet-shop.vercel.app)
NEXTAUTH_URL="https://your-project.vercel.app"

# ADMIN AUTHENTICATION (Custom JWT)
# Generate strong random strings for these
ACCESS_TOKEN_SECRET=""
REFRESH_TOKEN_SECRET=""

# OAUTH PROVIDERS
# Get these from Google Cloud Console > APIs & Services > Credentials
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# APP CONFIG
# Base URL for public assets/links
NEXT_PUBLIC_APP_URL="https://your-project.vercel.app"

# DEMO MODE
# Ensure these are empty or demo keys for production if still in demo phase
RAZORPAY_KEY_ID=""
RAZORPAY_KEY_SECRET=""
